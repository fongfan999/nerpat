$('.collection').append("<%= j render(@notifications) %>");

<% if @notifications.next_page %>
  $('.pagination').replaceWith('<%= j paginate(@notifications) %>');
<% else %>
  $('.pagination').remove();
<% end %>
