if ( $('#nerpat-requests ul li').length < 1 ) {
  $('#nerpat-requests')
    .html("<%= j render('notifications/nerpat_requests',
             nerpat_requests: @nerpat_requests) %>");

  <% @nerpat_requests.mark_as_read %>
}
