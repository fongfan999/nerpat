$('.collection').append("<%= j render(@nerpat_requests) %>");

<% if @nerpat_requests.next_page %>
  $('.pagination').replaceWith('<%= j paginate(@nerpat_requests) %>');
<% else %>
  $('.pagination').remove();
<% end %>
