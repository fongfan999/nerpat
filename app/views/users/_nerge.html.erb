<li class="collection-item avatar">
  <%= image_tag nerge.profile.avatar_image.url, class: "circle" %>
  <span class="title">
    <strong>
      <%= link_to nerge.ordered_name, nerge.profile, class: "black-text" %>
    </strong>
  </span>
  <p class="grey-text">
    <%= nerge.profile.username %><br/>
    <%= nerge.profile.major %> - <%= nerge.profile.school %>
  </p>
  
  <% if user_signed_in? && current_user.nerges.exists?(nerge.profile) %>
    <%= link_to [:remove_nerge, nerge], method: :delete,
      data: { tooltip: "Xoá nerge", delay: 10 },
      class: "secondary-content tooltipped red-text" do %>
      <i class="material-icons">clear</i>
    <% end %>
  <% else %>
    <%= link_to nerge.profile, class: "secondary-content nerge-text" do %>
      <i class="material-icons">send</i>
    <% end %>
  <% end %>
</li>